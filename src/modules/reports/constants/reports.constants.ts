/** Default max concurrent queued|running report jobs per tenant. */
export const DEFAULT_REPORT_MAX_CONCURRENT = 3;

/** Generated report file TTL. */
export const REPORT_TTL_HOURS = 24;

export const REPORT_ROW_LIMIT = 5000;
